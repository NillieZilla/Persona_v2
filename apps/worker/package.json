{
  "name": "@apps/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx --watch src/index.ts",
    "enqueue": "tsx src/enqueue.ts",
    "monitor": "tsx src/redis-monitor.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist || true"
  },
  "devDependencies": {
    "rimraf": "^5.0.7"
  },
  "dependencies": {
    "@pkg/core": "workspace:^",
    "bullmq": "^5.58.2",
    "dotenv": "^16.6.1",
    "ioredis": "^5.7.0",
    "pino": "^9.9.0",
    "pino-pretty": "^13.1.1",
    "zod": "^4.1.4"
  }
}
